{
  "name": "@campaigncell/auth-kit",
  "version": "1.0.0",
  "description": "Shared JWT verification guard and RBAC decorator/guard used independently by every CampaignCell service (defense-in-depth: each service verifies JWTs itself, not just the gateway).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "peerDependencies": {
    "@campaigncell/shared-types": "^1.0.0",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@campaigncell/shared-types": "^1.0.0",
    "@nestjs/common": "^10.4.4",
    "@nestjs/core": "^10.4.4",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^20.14.2",
    "rxjs": "^7.8.1",
    "typescript": "^5.4.5"
  }
}
